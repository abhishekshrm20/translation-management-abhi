name: admin_web_app
description: Admin UI for managing translations.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  uuid: ^4.0.0 # For generating IDs

  translation_domain: # Local package dependency
    path: ../packages/translation_domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0