name: translation_domain
description: Shared domain models for the translation assignment.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  equatable: ^2.0.5
  # Add other common dependencies if needed

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0